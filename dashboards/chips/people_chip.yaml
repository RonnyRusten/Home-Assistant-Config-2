type: template
icon: mdi:account-group
icon_color: |-
  {% if is_state('group.all_people','home') %}
    green
  {% else %}
    orange 
  {% endif %}
content: ""
tap_action:
  action: navigate
  navigation_path: people
double_tap_action:
  action: none
hold_action:
  action: none
