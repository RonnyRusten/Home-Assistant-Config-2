type: template
card_mod: 
  style: |
    ha-card {
      --chip-box-shadow: none;
      --chip-background: none;
      --chip-spacing: 0;
      margin-left: -10px;
    }
icon: mdi:set-top-box
icon_color: |-
  {% if is_state('media_player.telia_box_loft', 'playing') %}
    green
  {% else %}
    disabled
  {% endif %}